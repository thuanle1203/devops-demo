sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t edlee2k/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true edlee2k/react-test npm test

after_success:
  - docker build -t edlee2k/multi-client ./client
  - docker build -t edlee2k/multi-nginx ./nginx
  - docker build -t edlee2k/multi-server ./server
  - docker build -t edlee2k/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push edlee2k/multi-client
  - docker push edlee2k/multi-nginx
  - docker push edlee2k/multi-server
  - docker push edlee2k/multi-worker
